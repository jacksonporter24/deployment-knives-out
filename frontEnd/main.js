let button = document.querySelector('button')

const buttonAlert = () => {
    alert('Apple would not allow characters to use iPhones if they were a villain in the film.')
}

button.addEventListener('click', buttonAlert)